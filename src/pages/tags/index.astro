---
import Base from "../../layouts/Base.astro";
import { getArchivePosts } from "../../lib/archive_data";
import { getAllTags } from "../../lib/tags";
import { info } from "../../lib/info";

const posts = await getArchivePosts();
const tags = getAllTags(posts);
---

<Base title={`Tags - ${info.site.name}`} description="All topics and tags across articles.">
  <section class="container mx-auto px-6 pt-16 pb-20">
    <div class="max-w-3xl mx-auto">
      <header class="mb-8">
        <h1 class="text-3xl font-bold tracking-tight sm:text-4xl">Tags</h1>
        <p class="mt-3 text-base leading-relaxed text-muted-foreground">
          {tags.length} {tags.length === 1 ? 'topic' : 'topics'} across all articles.
        </p>
      </header>
      <ul class="divide-y divide-border/40 border-t border-border/40">
        {tags.map((tag) => (
          <li>
            <a
              href={`/tags/${tag.slug}`}
              class="group flex items-baseline justify-between gap-4 py-3.5 transition-colors"
            >
              <span class="font-mono text-sm text-foreground transition-colors group-hover:text-accent">
                <span class="text-accent/70">#</span>{tag.name}
              </span>
              <span class="shrink-0 text-xs text-muted-foreground">
                {tag.count} {tag.count === 1 ? 'post' : 'posts'}
              </span>
            </a>
          </li>
        ))}
      </ul>
    </div>
  </section>
</Base>
